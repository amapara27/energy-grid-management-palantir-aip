/**
 * AI Assistant — query interpretation and response entry point.
 *
 * Validates: Requirements 8.1, 8.2, 8.3, 8.4, 8.5, 8.6
 */

import type { AssistantQuery, AssistantResponse } from "./types.js";

export async function handleQuery(query: AssistantQuery): Promise<AssistantResponse> {
  throw new Error("Not implemented");
}
